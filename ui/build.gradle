jar {
	from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}

task(startContainer) {
	// dependsOn(jar) // commented out to avoid IOExceptions

	doLast { println "running taskexec"
	// TODO do it like it should be done
	exec {
		commandLine 'java -cp build/libs/ui-1.0.jar jade.Boot -agents ui:eu.ammw.msc.plaga.exec.UIAgent'
	}}
}
